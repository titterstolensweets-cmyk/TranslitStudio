# Ignore Windows metadata
desktop.ini
# API keys
api_keys.txt
# Debug output files
openai_debug.txt
*_debug.txt
# Utility scripts (personal development)
fix_checkboxes.py
# Local working file extracted from the archived manual branch
_legacy_MANUAL.md
# Superdocs (GitBook) documentation is tracked in this repo
# Translation memory JSON in root (test data)
/translation_memory.json
# Dev mode flag - local developer file only
.supervertaler.local
# SECURITY: Private data folder - NEVER sync to GitHub
user_data_private/
user_data_private.BAK/
# SAFETY: Archive folder for deleted files - NEVER sync to GitHub
# Located at C:\Dev\Supervertaler_Archive (outside this repo)
# Files deleted via safe_archive.ps1 go here for recovery
# Chat history logs - keep private (may contain sensitive development info)
docs/sessions/copilot_chat_history*.txt
docs/sessions/cursor_chat_history*.txt
docs/chat-logs/
# Private system prompts folder - never sync to GitHub
user data/System_prompts_private/
# Private custom instructions folder - never sync to GitHub
user data/Custom_instructions_private/
# Private projects folder - never sync to GitHub
user data/Projects_private/
# OLD folder names (deprecated, but ignore if they reappear)
custom_prompts_private/
projects_private/
# PyInstaller build artifacts (but allow docs/build/)
/build/
dist/
*.spec.bak
# Release packages (created by build_release.ps1)
*.zip
!docs/**/*.zip
# Icons (keep specific icons, ignore others)
*.ico
!assets/icon.ico
!docs/favicon.ico
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
# C extensions
*.so
# Distribution / packaging
.Python
/build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
# Build metadata - ALWAYS IGNORE
Supervertaler.egg-info/
# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# PyInstaller spec files that ARE part of this repo
!Supervertaler.spec
!Supervertaler.core.spec
!Supervertaler.full.spec
!Supervertaler_macOS.spec

# Release tooling that IS part of this repo
!create_release_zip.py

# Local build venvs (created by build scripts)
.venv-build/
.venv-build-core/
.venv-build-full/
.venv-build-*/
.venv_smoke/

# Windows artifacts
nul

# Local smoke-test folders
_smoketest*/
# Installer logs
pip-log.txt
pip-delete-this-directory.txt
# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/
# Translations
*.mo
*.pot
# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
# Flask stuff:
instance/
.webassets-cache
# Scrapy stuff:
.scrapy
# Sphinx documentation
docs/_build/
# PyBuilder
.pybuilder/
target/
# Jupyter Notebook
.ipynb_checkpoints
# IPython
profile_default/
ipython_config.py
# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version
# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock
# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#uv.lock
# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock
# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/latest/usage/project/#working-with-version-control
.pdm.toml
.pdm-python
.pdm-build/
# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/
# Celery stuff
celerybeat-schedule
celerybeat.pid
# SageMath parsed files
*.sage.py
# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
# Spyder project settings
.spyderproject
.spyproject
# Rope project settings
.ropeproject
# mkdocs documentation
/site
# mypy
.mypy_cache/
.dmypy.json
dmypy.json
# Pyre type checker
.pyre/
# pytype static type analyzer
.pytype/
# Cython debug symbols
cython_debug/
# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/
# Ruff stuff:
.ruff_cache/
# PyPI configuration file
.pypirc
# Release artifacts folder (build scripts, release guides, etc.)
.release_artifacts/

# Temp files (test imports, debug zips, etc.)
temp_*.zip

# SpecStory auto-generated chat files
.specstory/
docs/SpecStory/

# Local website dev/build folders (not part of this repo)
.astro/
node_modules/
michaelbeijer.co.uk/
michaelbeijer.co.uk-site/

# User data - runtime generated files (should never be in git)
# Note: user_data_private/ is already fully ignored above
user_data/web_cache/
user_data/Projects/
user_data/ai_assistant/
user_data/dictionaries/
user_data/model_version_cache.json
user_data/prompt_library/
user_data/recent_projects.json
user_data/resources/
user_data/shortcuts.json
user_data/snippet_library/
user_data/superbrowser_profiles/
user_data/themes.json
user_data/translation_memory.db
user_data/ui_preferences.json
user_data/voice_commands.json

# Temp dev scripts and notes
temp_*.py
.dev/scripts/analyze_memoq_*.py
issue_*_reply.md
plan.md

# Claude worktrees
.claude/worktrees/
.claude/launch.json
.claude/settings.local.json

# Private dev notes / scratch — NEVER commit (may contain user names, drafts)
.dev/

# PyPI / pip build artefacts (wheels + sdists)
dist-pypi/
pip_dist/

# Generated GitHub release-body / release-notes drafts
release-body-*.md
.release-notes-*.md

# SECURITY: client / working translation data — NEVER commit
*.svexport.json
*_bilingual.txt
*_bilingual.txt.svexport.json
